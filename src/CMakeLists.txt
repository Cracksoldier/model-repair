add_library(modelrepair SHARED
    Mesh.cpp
    RepairPipeline.cpp
    RepairReport.cpp
    repairs/MergeDuplicateVertices.cpp
    repairs/RemoveDegenerateTriangles.cpp
    repairs/FixNonManifold.cpp
    repairs/FixNormals.cpp
    repairs/FillHoles.cpp
    repairs/RemoveSelfIntersections.cpp
    repairs/Decimate.cpp
    repairs/Remesh.cpp
    repairs/Smooth.cpp
    repairs/ShellSeparation.cpp
    repairs/RemoveInternalGeometry.cpp
    repairs/RemoveInternalGeometryStep.cpp
    repairs/Subdivide.cpp
    analysis/WallThickness.cpp
    io/StlIO.cpp
    io/ObjIO.cpp
    io/ThreeMFIO.cpp
    io/GlbIO.cpp
    io/MeshIO.cpp
)

target_include_directories(modelrepair
    PUBLIC
        $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/include>
        $<BUILD_INTERFACE:${CMAKE_BINARY_DIR}/include>
        $<INSTALL_INTERFACE:include>
)

target_include_directories(modelrepair
    PRIVATE
        # lib3mf only sets PRIVATE includes on its own target, so we add the
        # C++ bindings header path explicitly.
        ${lib3mf_SOURCE_DIR}/Autogenerated/Bindings/Cpp
)

target_link_libraries(modelrepair
    PUBLIC
        CGAL::CGAL
        CGAL::CGAL_Core
        Eigen3::Eigen
    PRIVATE
        lib3mf
        tinygltf
        modelrepair_warnings
)

if(TBB_FOUND AND MODELREPAIR_ENABLE_TBB)
    target_link_libraries(modelrepair PRIVATE TBB::tbb)
    target_compile_definitions(modelrepair PRIVATE MODELREPAIR_HAVE_TBB)
endif()

if(Vulkan_FOUND AND MODELREPAIR_ENABLE_VULKAN)
    target_link_libraries(modelrepair PRIVATE Vulkan::Vulkan)
    target_compile_definitions(modelrepair PRIVATE MODELREPAIR_HAVE_VULKAN)
    target_sources(modelrepair PRIVATE repairs/VulkanSmoothing.cpp)

    set(SHADER_SRC ${CMAKE_CURRENT_SOURCE_DIR}/shaders/smooth_laplacian.comp)
    set(SHADER_SPV ${CMAKE_CURRENT_BINARY_DIR}/smooth_laplacian.spv)
    set(SHADER_HDR ${CMAKE_CURRENT_BINARY_DIR}/smooth_laplacian_spv.h)

    add_custom_command(
        OUTPUT  ${SHADER_SPV}
        COMMAND ${GLSLC_EXECUTABLE} --target-env=vulkan1.0
                -o ${SHADER_SPV} ${SHADER_SRC}
        DEPENDS ${SHADER_SRC}
        COMMENT "Compiling smooth_laplacian.comp → SPIR-V"
    )
    add_custom_command(
        OUTPUT  ${SHADER_HDR}
        COMMAND ${CMAKE_COMMAND}
                -DINPUT_FILE=${SHADER_SPV}
                -DOUTPUT_FILE=${SHADER_HDR}
                -DARRAY_NAME=smooth_laplacian_spv
                -P ${CMAKE_SOURCE_DIR}/cmake/EmbedSpv.cmake
        DEPENDS ${SHADER_SPV}
        COMMENT "Embedding SPIR-V into smooth_laplacian_spv.h"
    )
    target_sources(modelrepair PRIVATE ${SHADER_HDR})
    target_include_directories(modelrepair PRIVATE ${CMAKE_CURRENT_BINARY_DIR})
endif()

set_target_properties(modelrepair PROPERTIES
    VERSION   ${PROJECT_VERSION}
    SOVERSION ${PROJECT_VERSION_MAJOR}
)

install(TARGETS modelrepair
    LIBRARY DESTINATION lib COMPONENT Runtime
    ARCHIVE DESTINATION lib COMPONENT Runtime
)
